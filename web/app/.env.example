VITE_APP_URL=http://localhost:5174
VITE_SITE_URL=http://localhost:5173
# Leave blank in dev: vite proxies /trips, /approvals, /disruptions, /simulate,
# /health and /ws to :8000.
# Set it for a built bundle served from a different origin than the API.
VITE_API_URL=
