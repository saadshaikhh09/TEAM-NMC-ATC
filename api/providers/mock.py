"""Mock providers. Owner: Person B.

THIS IS THE DEMO DEFAULT. Venue wifi is the failure you will actually face,
and this is the only thing that survives it.

Rules:
  - Deterministic. No random(). You will rehearse 30 times and every run must
    be identical or you cannot script the pitch.
  - search() must return a set that produces at least one interesting rejection
    for Priya: something cheaper and faster that lands after 09:00.
"""
raise NotImplementedError("B2")
